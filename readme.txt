The project supports next features:
- Different browsers: firefox and chrome browser. You can specify them in the /src/main/resources/config.properties file
- Selenium Grid for remote executing
- For reporting uses the Surefire-reports library
- Can get screenshots if any test will not pass (it puts them into the report)